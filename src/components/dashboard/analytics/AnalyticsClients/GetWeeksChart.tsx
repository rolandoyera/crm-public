const dateOptions = {
    month: "short",
    day: "numeric",
}
let d = new Date()

export const today = new Date().toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)

export const minusOneWeek = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusTwoWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusThreeWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusFourWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusFiveWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusSixWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusSevenWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusEightWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusNineWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusTenWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusElevenWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusTwelveWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusThirteenWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
export const minusFourteenWeeks = new Date(
    d.setDate(d.getDate() - 7)
).toLocaleDateString(
    "en-US",
    //@ts-ignore
    dateOptions
)
